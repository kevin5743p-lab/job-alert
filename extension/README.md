# JobCopilot — Auto-Apply (extension)

Documentation lives one level up, in [`../README.md`](../README.md) — setup, how
a single application runs, the grounding contract, site tiers, and cost.

> The README that used to sit here described the tailor-only build and said the
> tool "deliberately refuses to submit anything". That is no longer true of this
> folder, which is why it isn't kept here. The original is intact and still
> accurate at `../../extension/README.md`.
